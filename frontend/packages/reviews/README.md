# @tribunal/reviews

Extracted package for the **reviews** block.

> Public API mirrors `docs/blocks/reviews/BLOCK.md` (`public_api`). Fill this in
> as the block's source is moved into `src/` during extraction.

## Install

```bash
npm install @tribunal/reviews
```

## Public API

_TODO: document the exports from `src/index.ts`, mirroring the block's
`public_api` surface in `docs/blocks/reviews/BLOCK.md`._

## Build

```bash
npm run build      # tsup -> dist/ (esm + d.ts)
npm run typecheck  # tsc --noEmit
```
